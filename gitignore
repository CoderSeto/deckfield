# The database is a build artifact, not a source file -- always
# regenerable from migrate_s9.py (the workbook) + results/*.csv
# (deckfield add-results, replayed in order). Never commit it.
deckfield.db
deckfield_test*.db
deckfield_*_backup*.db
*.db-journal
*.db-wal
*.db-shm

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS junk
.DS_Store
Thumbs.db

# Editor files
*.swp
*~
